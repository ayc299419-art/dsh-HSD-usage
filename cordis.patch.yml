# dsh-HSD-usage（涵盛达API用量查询）—— profile 注册层。
# 由 `dsh plugin --profile web add github:ayc299419-art/dsh-HSD-usage` 自动应用；
# 也兼容手动复制到 profile 的 cordis.patch.yml。
- insert:
    - id: hsd-usage
      name: 'dsh-HSD-usage'
